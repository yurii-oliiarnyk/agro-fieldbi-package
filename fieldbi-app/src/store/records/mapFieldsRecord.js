import { Record } from 'immutable';

const MapFieldsRecord = Record({
  id: null,
  name: null,
  coordinates: null,
  exactCoordinates: null,
  exactCoordinatesLoading: null
});

export default MapFieldsRecord;
