import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrap: {
    backgroundColor: '#fff',
    flex: 1,
    padding: 16,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
